using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Objeto : MonoBehaviour
{

    public Vector3 algo; 
    // Start is called before the first frame update
    void Start()
    {
        //algo = new Vector3(0f, 0f, 0f); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
